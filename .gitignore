# 專案專屬產出目錄（data/ 掃描原始資料、findings/ 分析結果、report/ 報告、archive/ 各期存檔）
# 含專案內部資訊，只留本機、不提交內容。改由各目錄內的 .gitignore 保留「空目錄結構」進版控
# （clone 後目錄即存在），內容則全部忽略——見 data/、findings/、report/、archive/、tmp/ 的 .gitignore

# macOS
.DS_Store

# 定價查詢的 service ID 對照表（pricing-lookup.sh 用到才查、查過自動寫回）：
# 內容不含機密，長期進版控是合理的，但這個專案目前仍是 template 階段，
# 先不提交（避免帶著測試期間查到的內容進 template）。等專案正式用於特定情境時，
# 使用者會另外指示改回進版控——屆時只要把這一行拿掉即可，不影響 pricing-lookup.sh 的邏輯。
.claude/skills/report-gcp/references/pricing-service-ids.json
