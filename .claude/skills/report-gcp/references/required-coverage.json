{
  "_doc": "必掃清單（政策斷言，不執行任何 gcloud）。列在 required 的 assetType 若『存在於專案卻沒被 scan.sh 掃到』，coverage-check.sh 會從『待分類缺口』升級成硬失敗（exit 1），擋下整條 digest。用途有二：(1) 保護 findings 所依賴的關鍵型別不因未來重構而無聲掉出覆蓋（回歸保護）；(2) 要新增必掃時，把型別加進這裡→管線刻意失敗→逼人把它加進 scan.sh→缺口消失。此檔進版控才有稽核軌跡（哪一期把什麼設成必掃）。covered 判定以 coverage-check.sh 內嵌的 COVERED 表為準。",
  "required": [
    "compute.googleapis.com/Instance",
    "compute.googleapis.com/Firewall",
    "compute.googleapis.com/BackendService",
    "compute.googleapis.com/SecurityPolicy",
    "sqladmin.googleapis.com/Instance",
    "redis.googleapis.com/Instance",
    "redis.googleapis.com/Cluster",
    "storage.googleapis.com/Bucket",
    "iam.googleapis.com/ServiceAccount",
    "iam.googleapis.com/ServiceAccountKey",
    "apikeys.googleapis.com/Key"
  ]
}
